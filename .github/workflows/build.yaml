name: Hello World
 
 
on:
  push:
    branches: [ "main" ] 
  pull_request:
    branches: [ "main" ] 
 
jobs:
  hello-world-job:
    runs-on: ubuntu-latest
 
    steps:
    - name: Print Hello World
      run: echo "Hello, World!"
      
    - name: Build the Docker image
      run: docker build ./ArtistApi -t amardeep/newimage